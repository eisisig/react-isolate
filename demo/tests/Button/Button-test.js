'use strict';

const describe = require('tape-bdd');

describe('Button', function ( it ) {

	it('should be clickable', function ( assert ) {
		assert.true(true)
	});

	it('should be disabled', function ( assert ) {
		assert.false(false);
	});

});
